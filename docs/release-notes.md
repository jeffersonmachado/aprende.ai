# Release Notes

## 2.1.0
- Jornada unificada consolidada com `journey-runtime` backend-first e `journey-engine` operacional em sete fases explícitas.
- Jornada adaptativa promovida a experiência canônica em `/adaptive-journey`, com redirects compatíveis, entrada no menu principal e integração contextual com dashboard e campanha.
- Cobertura validada com API completa `21 suites / 99 testes`, frontend `24 arquivos / 70 testes`, suíte adaptativa backend `4 suites / 15 testes` e Playwright completo com `24 cenários`.
- Pipeline de entrega endurecido com ZIP versionado, checksum persistido, manifesto JSON, índice consolidado, changelog legível e critério de publicação limpa.